# Test-only upstream in a disposable, network-isolated container. No real backend/DB.
server {
    listen 3520;
    location / {
        default_type application/json;
        return 200 '{"status":"ok","uri":"$request_uri","method":"$request_method"}';
    }
}
